opencv-python==4.10.0.84
numpy==1.26.4
scikit-learn>=1.3
scipy>=1.11
anthropic>=0.30
requests>=2.32.5,<3.0.0
# optional - only needed for the LLM-backed planning path. Without it (or
# without ANTHROPIC_API_KEY set) the pipeline runs fine on the heuristic
# fallback planner.
